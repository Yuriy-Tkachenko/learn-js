const user = {
    name: "John"
  };
  
  // это будет работать?
  user.name = "Pete";

  // Это работает, так как через const мы указаываем, 
  // что имя переменной не будет изменяться, менять значения ключей в объекте таким образом возможно